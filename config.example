# 自动登录服务配置文件示例
#
# 此文件展示了所有可配置的选项
# 实际配置文件会在安装时自动生成到：/etc/config/autologin

# ==================== 网络配置 ====================

# WAN 接口名称
# 常见值：eth1, wan, eth0.2, pppoe-wan
# 安装程序会自动检测，通常无需修改
WAN_INTERFACE="eth1"

# ==================== 登录凭证 ====================

# 登录账号（必填）
USER_ACCOUNT="your_student_id"

# 登录密码（必填）
USER_PASSWORD="your_password"

# 运营商选择
# 1 = 联通
# 2 = 移动
ISP_CHOICE="1"

# ==================== 监测配置 ====================

# 网络检测频率（单位：毫秒）
# 建议值：
#   - 高频监测：10000 (10秒) - 适合需要快速重连的场景
#   - 标准监测：30000 (30秒) - 推荐，平衡性能和及时性
#   - 低频监测：60000 (60秒) - 适合稳定网络，减少系统负载
CHECK_INTERVAL="30000"

# ==================== 日志配置 ====================

# 日志类型
# 1 = 文件日志（可配置大小限制）
# 2 = syslog（系统日志）
# 3 = 禁用日志（输出到 /dev/null）
LOG_TYPE="1"

# 日志文件路径（LOG_TYPE=1 时有效）
LOG_FILE="/usr/local/autologin/logs/autologin.log"

# 日志文件大小限制（单位：MB）
# 当日志文件超过此大小时，会自动轮转
# 旧日志会被重命名为 *.log.old
# 建议值：5-20 MB
LOG_SIZE_MB="10"

# ==================== 配置示例 ====================

# 示例 1: 宿舍网络（稳定）
# WAN_INTERFACE="eth1"
# CHECK_INTERVAL="60000"     # 60秒检测一次
# LOG_TYPE="1"               # 文件日志
# LOG_SIZE_MB="5"            # 小日志文件

# 示例 2: 移动热点（不稳定）
# WAN_INTERFACE="wlan0"
# CHECK_INTERVAL="10000"     # 10秒检测一次
# LOG_TYPE="2"               # syslog，便于远程查看
# ISP_CHOICE="2"             # 移动运营商

# 示例 3: 性能优先（低开销）
# WAN_INTERFACE="eth1"
# CHECK_INTERVAL="60000"     # 60秒检测一次
# LOG_TYPE="3"               # 禁用日志

# ==================== 注意事项 ====================

# 1. 修改配置后需要重启服务才能生效：
#    /etc/init.d/autologin restart

# 2. 密码中如果包含特殊字符，请确保正确转义
#    例如：USER_PASSWORD="pass\"word"

# 3. 检测频率不宜过高，建议不低于 10000 ms（10秒）
#    过高频率会增加系统负载和网络请求

# 4. 日志大小建议根据存储空间调整：
#    - 存储充足：10-20 MB
#    - 存储紧张：5 MB 或使用 syslog

# 5. 如果需要调试，建议临时启用 syslog：
#    LOG_TYPE="2"
#    然后使用 logread -f | grep autologin 实时查看
